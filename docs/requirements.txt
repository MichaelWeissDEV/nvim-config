# Documentation build dependencies.
#
# Used by Read the Docs (see /.readthedocs.yaml) and by
# scripts/docs-build.sh / docs-build.ps1 when creating docs/.venv.
# Keep in sync with pyproject.toml's [project.optional-dependencies].docs.
sphinx>=9.1.0,<10
myst-parser>=5.1.0,<6
sphinx-rtd-theme>=3.1.0,<4
